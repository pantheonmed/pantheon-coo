# requirements-dev.txt — install with: pip install -r requirements-dev.txt
# Includes all prod deps + dev tooling

-r requirements.txt

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
pytest-timeout==2.3.1

# Code quality
ruff==0.4.4
mypy==1.10.0
pre-commit==3.7.1

# JWT support (for AUTH_MODE=jwt)
PyJWT==2.8.0

# Type stubs
types-requests==2.32.0.20240521
